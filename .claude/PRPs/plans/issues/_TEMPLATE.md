<!--
ISSUE BODY TEMPLATE for sub-issues. DO NOT post this file as an issue.

Frontmatter is consumed by the issue-creation script:
  title:     The exact GitHub issue title (English).
  spec_id:   §15 spec ID, e.g. P2-01.
  phase:     P2 / P3 / ... — drives the phase:* label and the milestone.
  labels:    YAML list of `category:value` strings. Will all be applied at creation.
  milestone: One of v0.2.0 — Workspace + PDF | v0.3.0 — Grading | ...
  deps:      YAML list of upstream spec IDs, e.g. ["P2-01", "P2-02"].
-->
---
title: "Convert repository to a Cargo workspace"
spec_id: "P2-01"
phase: "P2"
labels:
  - phase:P2
  - area:infra
  - type:refactor
milestone: "v0.2.0 — Workspace + PDF"
deps: []
---

> ℹ️ **Reference draft.** This issue body was generated by the Claude Code planner agent from the master plan ([`.claude/PRPs/plans/shalgalt-omr-master.plan.md`](.claude/PRPs/plans/shalgalt-omr-master.plan.md), §15). Treat it as a starting point — verify scope, acceptance criteria, and implementation notes before work begins.

## Context

Two short paragraphs explaining *why* this issue exists, what problem it solves, and where it fits inside the phase. Reference the relevant master-plan sections (`§5`, `§6`, `§8`, …) inline.

## Acceptance Criteria

- [ ] Concrete, observable outcome 1
- [ ] Concrete, observable outcome 2
- [ ] Concrete, observable outcome 3

## Implementation Notes

Concrete pointers:
- Files to create / move / edit, with paths.
- Libraries / crates to add, with version constraints.
- Patterns to follow (link master-plan section).
- Tricky edges / gotchas worth flagging.

## Mandatory Reading

| Path | Why |
| --- | --- |
| `AGENTS.md` | Repo rules (Rules 1–4, language conventions). |
| `.claude/PRPs/plans/shalgalt-omr-master.plan.md` §N | Specific section relevant to this issue. |
| `path:lines` | Specific reason. |

## Validation

```bash
# Concrete commands to run that prove the work is done.
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo check --workspace --all-targets --all-features
```

EXPECT: zero clippy warnings, build green, …

## Dependencies

- Upstream (must be complete first): _none_ | `#P2-01`, …
- Downstream (informational): `#P2-02`, …

## Out of scope

- Any item explicitly NOT to be done in this issue.

