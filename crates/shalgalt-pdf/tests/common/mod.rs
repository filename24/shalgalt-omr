//! `tests/common/` collects helpers shared between integration test binaries — the
//! standard Cargo pattern. `tests/golden.rs` and `tests/preset_smoke.rs` import this
//! module via `mod common;`.

pub mod preset;
